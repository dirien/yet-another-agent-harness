// Package handlers provides concrete hook handler implementations for yaah.
// Built-in handlers include CommandGuard (blocks dangerous shell commands),
// Linter (enforces code standards via profiles), SecretScanner (detects
// hardcoded secrets), CommentChecker (flags placeholder comments), and
// SessionLogger (logs session lifecycle events).
package handlers
